// Por padrão, o JEST não foi criado para fazer testes na DOM (verificar existência de elementos)
// O arquivo é executado junto com os testes, para que a importação seja sempre feita.
import '@testing-library/jest-dom'